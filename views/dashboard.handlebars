<title>Dashboard - Loja de Brigadeiros</title>
<!-- Bootstrap CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="/css/style-home.css">

<!-- Navbar -->
<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand">Olá, {{usuario.nome}}</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="#sabores">Sabores</a>
        <a class="nav-link" href="#encomendas">Encomendas</a>
        <a class="nav-link" href="#encomendas">Administradores</a>
        <a class="nav-link" href="/login/logout">Logout</a>
      </div>
    </div>
  </div>
</nav>
  
<div class="container mt-4">
    <div class="container mt-4">
        <h1>Gerenciar Usuários</h1>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Email</th>
                    <th>Ações</th>
                </tr>
            </thead>
            {{#each listaUsuarios}}
            <tbody>
                <!-- Exemplo de linha de usuário -->
                <tr>
                    <td>{{this.nome}}</td>
                    <td>{{this.email}}</td>
                    <td>
                        <a href="/vizualizar/editar/{{this.id}}" class="btn btn-primary btn-edit">Editar</a>
                        <button onclick="deletarUsuario({{this.id}})" class="btn btn-danger btn-delete">Excluir</button>
                    </td>
                </tr>
                <!-- Adicione mais linhas aqui -->
            </tbody>
            {{/each}}
            <tbody>
                <!-- Exemplo de linha de usuário -->
                <tr>
                    <td>Arthur</td>
                    <td>rutu.narciso@gmail.com</td>
                    <td>
                        <a href="/vizualizar/editar/{{this.id}}" class="btn btn-primary btn-edit">Editar</a>
                        <button onclick="deletarUsuario({{this.id}})" class="btn btn-danger btn-delete">Excluir</button>
                    </td>
                </tr>
                <!-- Adicione mais linhas aqui -->
            </tbody>
        </table>
    </div>
</div>

<div class="container mt-4">
    <div class="container mt-4">
        <h1>Gerenciar Encomendas</h1>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Cliente</th>
                    <th>Sabores</th>
                    <th>Data</th>
                    <th>Hora</th>
                    <th>Tipo</th>
                    <th>Quantidade</th>
                    <th>Para Entrega</th>
                    <th>Endereço</th>
                    <th>Ações</th>
                </tr>
            </thead>
            {{#each listaEncomendas}}
            <tbody>
                <!-- Exemplo de linha de usuário -->
                <tr>
                    <td>{{this.cliente}}</td>
                    <td>{{this.sabores}}</td>
                    <td>{{this.data_retirada}}</td>
                    <td>{{this.hora_retirada}}</td>
                    <td>{{this.tipo}}</td>
                    <td>{{this.quantidade}}</td>
                    <td>{{this.para_entrega}}</td>
                    <td>{{this.endereco}}</td>

                    <td>
                        <a href="/vizualizar/editar/{{this.id}}" class="btn btn-primary btn-edit">Concluir</a>
                        <button onclick="deletarUsuario({{this.id}})" class="btn btn-danger btn-delete">Excluir</button>
                    </td>
                </tr>
                <!-- Adicione mais linhas aqui -->
            </tbody>
            {{/each}}
            <tbody>
                <!-- Exemplo de linha de usuário -->
                <tr>
                    <td>Fábio Santos</td>
                    <td>
                        Brigadeiro Tradicional
                        Brigadeiro de Oreo
                    </td>
                    <td>28/11/2024</td>
                    <td>14:00</td>
                    <td>Cento</td>
                    <td>3</td>
                    <td>Sim</td>
                    <td>Rua Dr. Mário Jardim Freire, 535, Boa vista 1</td>

                    <td>
                        <a href="/vizualizar/editar/{{this.id}}" class="btn btn-primary btn-edit">Concluir</a>
                        <button onclick="deletarUsuario({{this.id}})" class="btn btn-danger btn-delete">Excluir</button>
                    </td>
                </tr>
                <!-- Adicione mais linhas aqui -->
            </tbody>
        </table>
    </div>
</div>


  <div class="container mt-5">
    <h2>Sabores Cadastrados</h2>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>#</th>
          <th>Nome</th>
          <th>Imagem</th>
          <th>Descrição</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>1</td>
          <td>Brigadeiro Tradicional</td>
          <td><img style="height: 100px; width: 100px;" src="/assets/brigadeiro.jpeg" alt="Produto A" class="img-fluid"></td>
          <td>Descrição do produto A</td>
        </tr>
        <tr>
          <td>2</td>
          <td>Produto B</td>
          <td><img src="https://via.placeholder.com/50" alt="Produto B" class="img-fluid"></td>
          <td>Descrição do produto B</td>
        </tr>
        <tr>
          <td>3</td>
          <td>Produto C</td>
          <td><img src="https://via.placeholder.com/50" alt="Produto C" class="img-fluid"></td>
          <td>Descrição do produto C</td>
        </tr>
      </tbody>
    </table>
  </div>


<script>
    const api = axios.create({
        baseURL: '{{baseURL}}',
        timeout: 10000,
        headers: {
            'Content-Type': 'application/json',
        },
    });

    function deletarUsuario(id) {
        if (window.confirm('Tem certeza que deseja deletar este usuário?')) {
            axios.delete(`/cadastrar/usuario/deletar/${id}`)
                .then(response => {
                    window.alert(response.data.message);
                    // Forçar a atualização da página
                    window.location.href = "/vizualizar/usuarios";
                })
                .catch(error => {
                    console.error('Erro ao deletar usuário:', error);
                    alert('Ocorreu um erro ao deletar o usuário.');
                });
        } else {
            console.log('Operação de conclusão cancelada.');
        }
    }

</script>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

